{	"_comment": "This file defines properties UglifyJS shouldn't mangle. Run as:",
	"_comment": "uglifyjs --mangle --mangle-proprs --reserved-file uglifyjs.js --comments --compress unsafe jzsip.js -o jzsip.min.js",

	"props": ["read_raw", "read_utf8", "read_base64"]
}
